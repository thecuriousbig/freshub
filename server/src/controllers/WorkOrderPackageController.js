const { WorkOrderPackage } = require('../models')
const { Product } = require('../models')


module.exports = {
  async getWorkOrderPackageByID(req, res) {
    // try to get PurchaseOrder By ID
    
  },
}
